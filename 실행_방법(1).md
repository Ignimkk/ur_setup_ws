# 실행 방법

## 1. ROS 2 환경 적용

```bash
source /opt/ros/humble/setup.bash
source ~/dev_ws/robot_arm/UR/ur_setup_ws/install/setup.bash
```

Robotiq 드라이버 설치 확인:

```bash
ros2 pkg prefix robotiq_driver
```

---

## 2. 실물 로봇 네트워크 설정

### 2.1 UR16e 컨트롤박스 설정

Teach Pendant에서 다음 메뉴로 이동합니다.

```text
Setup Robot → Network
```

정적 IP를 설정합니다.

```text
IP Address : 192.168.56.101
Subnet Mask: 255.255.255.0
Gateway    : 비워두거나 현재 네트워크에 맞게 설정
```

### 2.2 PC 유선 네트워크 설정

PC의 UR 전용 유선 NIC를 다음과 같이 설정합니다.

```text
IPv4 Address: 192.168.56.10
Netmask     : 255.255.255.0
Gateway     : 비워둠
```

Ubuntu 설정 경로:

```text
Settings → Network → Wired → IPv4 → Manual
```

설정 후 연결을 확인합니다.

```bash
ip addr
ping 192.168.56.101
```

`ping` 응답이 없으면 다음 항목을 확인합니다.

- PC와 UR 컨트롤박스가 같은 서브넷인지
- Ethernet 케이블 연결 상태
- IP 주소 중복 여부
- PC 방화벽 설정

필요 시 테스트 목적으로 방화벽 상태를 확인합니다.

```bash
sudo ufw status
```

---

## 3. External Control URCap 설치

External Control URCap은 최초 한 번만 설치하면 됩니다.

1. `ur_robot_driver`에 포함된 다음 파일을 USB로 복사합니다.

```text
external_control-*.urcap
```

파일은 일반적으로 `ur_robot_driver/resources` 또는 설치된 패키지 경로에서 찾을 수 있습니다.

```bash
ros2 pkg prefix ur_robot_driver
```

2. USB를 Teach Pendant에 연결합니다.
3. 다음 메뉴로 이동합니다.

```text
Setup Robot → URCaps → +
```

4. `external_control-*.urcap` 파일을 선택해 설치합니다.
5. 설치 완료 후 로봇 컨트롤러를 재부팅합니다.

---

## 4. External Control 설정

Teach Pendant에서 다음 메뉴로 이동합니다.

```text
Installation → URCaps → External Control
```

다음 값을 설정합니다.

```text
Host IP: 192.168.56.10
```

여기서 Host IP는 UR 로봇 IP가 아니라 **ROS 2 PC의 유선 NIC IP**입니다.

Host name과 Custom Port는 특별한 이유가 없다면 기본값을 유지합니다.

설정 후 Installation 파일을 저장합니다.

---

## 5. External Control 프로그램 생성

Teach Pendant에서 새 프로그램을 생성합니다.

1. `Program` 메뉴로 이동
2. `URCaps` 항목에서 `External Control` 노드 추가
3. 프로그램 트리에 External Control 노드가 포함되었는지 확인
4. 프로그램 저장

실행 순서:

1. 로봇 전원 ON
2. Power ON
3. Brake Release
4. External Control 프로그램 열기
5. Teach Pendant에서 `Play` 버튼 실행
6. PC에서 ROS 2 launch 실행

`External Control` 프로그램이 실행되지 않으면 ROS 2 driver와 연결되더라도 실제 trajectory 실행이 제한될 수 있습니다.

---

## 6. 실물 UR16e + Robotiq 실행

Teach Pendant에서 External Control 프로그램을 `Play`한 뒤 PC에서 실행합니다.

```bash
ros2 launch ur_setup_bringup ur_real_moveit_robotiq_ur16e.launch.py \
  robot_ip:=192.168.56.101 \
  gripper_com_port:=/dev/ttyUSB0
```

기본값은 다음과 같습니다.

```text
activate_joint_controller:=false
```

따라서 launch 직후 trajectory controller가 활성화되지 않아 자동 모션이 차단됩니다.

컨트롤러 상태 확인:

```bash
ros2 control list_controllers
```

RViz에서 `Plan Only`로 궤적과 충돌 여부를 먼저 확인합니다.

확인이 끝난 후 trajectory controller를 활성화합니다.

```bash
ros2 control switch_controllers \
  --activate scaled_joint_trajectory_controller
```

다른 터미널에서 Pick & Place를 실행합니다.

```bash
source /opt/ros/humble/setup.bash
source ~/dev_ws/robot_arm/UR/ur_setup_ws/install/setup.bash

ros2 launch pick_place_module pick_place.launch.py use_sim:=false
```

---

## 7. 시뮬레이션 실행

터미널 1:

```bash
ros2 launch ur_setup_bringup ur_sim_moveit_robotiq_ur16e.launch.py
```

터미널 2:

```bash
ros2 launch pick_place_module pick_place.launch.py use_sim:=true
```

---

## 8. Robotiq 2F-85 연결

실물 모드에서는 UR과 Robotiq 하드웨어 인터페이스가 동일한 `controller_manager`에서 함께 로드됩니다.

별도의 Robotiq 전용 노드는 실행하지 않습니다.

기본 USB-RS485 포트:

```bash
gripper_com_port:=/dev/ttyUSB0
```

장치명을 고정하려면 다음 udev rule을 설정할 수 있습니다.

```udev
# /etc/udev/rules.d/99-robotiq.rules
SUBSYSTEM=="tty", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6001", \
  SYMLINK+="robotiq", MODE="0666"
```

적용:

```bash
sudo udevadm control --reload-rules
sudo udevadm trigger
```

이후 다음 포트를 사용합니다.

```bash
gripper_com_port:=/dev/robotiq
```

Robotiq action 인터페이스:

```text
robotiq_gripper_controller/gripper_cmd
```

---

## 9. URDF 및 컨트롤러 구성

실물용 URDF는 기존 시뮬레이션 URDF를 복사하여 별도로 사용합니다.

실물용 URDF에서는 다음 항목을 제거하거나 비활성화합니다.

- Gazebo 플러그인
- RGB-D simulation sensor
- DetachableJoint
- simulation 전용 `ros2_control`

`pedestal`, `plate`, `pallet`은 실제 환경과 동일하므로 충돌 검사용으로 유지합니다.

단, 해당 구조물의 fixed joint는 `ros2_control` joint 목록에 포함하지 않습니다.

실물 MoveIt controller:

```text
scaled_joint_trajectory_controller
```

---

## 10. 실행 전 검증

```bash
ros2 topic echo /joint_states
ros2 control list_controllers
ros2 control list_hardware_interfaces
ros2 action list
```

권장 최초 실물 테스트 순서:

1. `ping`으로 네트워크 연결 확인
2. External Control 프로그램 `Play`
3. 실물 launch 실행
4. `/joint_states` 확인
5. RViz에서 현재 로봇 자세 확인
6. `Plan Only` 수행
7. pedestal, plate, pallet 충돌 확인
8. `scaled_joint_trajectory_controller` 활성화
9. 낮은 속도로 짧은 경로 실행
10. Pick & Place 실행
